Ext.define('{appName}.view.organization.Browse', {
    extend: '{appName}.view.widgets.Browse',

    fields: {
        manager: {
            property: 'manager_id'
        }
    },

    controller: 'organizationbrowse',
    viewModel: {
        type: 'organizationbrowse'
    },

    cls: 'organizationbrowse',
    bind: {
        store: '{organizations}'
    }
});
