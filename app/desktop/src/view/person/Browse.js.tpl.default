Ext.define('{appName}.view.person.Browse', {
    extend: '{appName}.view.widgets.Browse',

    fields: {
        office: {
            property: 'office_id'
        },
        organization: {
            property: 'organization_id'
        }
    },

    controller: 'personbrowse',
    viewModel: {
        type: 'personbrowse'
    },

    cls: 'personbrowse',
    bind: {
        store: '{people}'
    }
});
