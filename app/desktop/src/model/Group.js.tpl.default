/**
 * This class describes a group of users.
 */
Ext.define('{appName}.model.Group', {
    extend: '{appName}.model.Base',

    fields: [
        'name',
        { name: 'organizationId', reference: 'Organization' }
    ],

    manyToMany: 'User'
});
