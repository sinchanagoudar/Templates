Ext.define('{appName}.store.Events', {
    extend: 'Ext.data.Store',
    alias: 'store.events',

    model: '{appName}.model.Event',
    remoteFilter: true,
    remoteSort: true,
    autoLoad: false
});
