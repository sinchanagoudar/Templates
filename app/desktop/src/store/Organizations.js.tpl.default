Ext.define('{appName}.store.Organizations', {
    extend: 'Ext.data.Store',
    alias: 'store.organizations',

    model: '{appName}.model.Organization',
    remoteFilter: true,
    remoteSort: true,
    sorters: 'name'
});
