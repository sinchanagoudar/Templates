Ext.define('{appName}.store.People', {
    extend: 'Ext.data.Store',
    alias: 'store.people',

    model: '{appName}.model.Person',
    // remoteFilter: true,
    // remoteSort: true,
    // sorters: 'lastname'
});
